<%if current_user.is_admin?%>
<h1>Administrar Institución </h1>
<%= link_to 'Agregar institución', new_institution_path, :class=>"btn btn-lg btnAdmin" %><br>

<div class="panel panel-default panelRease">
	<div class="panel-heading">
		Listado de instituciones
	</div>
	<div class="panel-body">
		<% @institutions.each do |institution| %>
			<p><%=institution.name%> - 
			<%= link_to 'Ver detalle', institution %> -
			<%= link_to 'Web', institution.web, :target => "_blank" %> -
			<%= link_to 'Borrar institución', institution, method: :delete, data: { confirm: '¿Seguro que desea eliminar esta institución?' } %>
			</p>
		<%end%>
	</div>
</div>
<%else%>
	<h1>Organizaciones adheridas</h1>
	<p> Las organizaciones que actualmente están activas dentro de REASE o alguno de los miembros activos pertenecen a ellas, son: </p>

	<%if @institutions.present?%>
		<% @institutions.each do |institution| %>
			<%= link_to(image_tag(institution.logo.url(:mini)), institution, :id=>"logoInstituciones") %>
		<%end%>
	<%else%>
		Aún no se han agregado insituciones.
	<%end%>
<%end%>

