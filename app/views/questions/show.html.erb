<div  class="panel panel-default panel-rease">
	<div class="panel-heading">
		<%=	@question.title %>
	</div>
	<div class="panel-body">
		<h2>Tipo: <%= @question.reader %></h2>
		<p>	<%=raw @question.answer %></p>
	</div>
	<p>Fecha de Creación: <%=l @question.created_at%></p>			
</div>
<%if user_signed_in? and current_user.is_admin?%>
	<%= link_to "Editar", edit_question_path( @question), :class => "btn btn-lg btn-crud"%>
	<%= link_to 'Eliminar',  @question, method: :delete, data: { confirm: '¿Está seguro de eliminar esta pregunta?' }, :class => "btn btn-lg btn-crud" %>
	<%= link_to "Volver", questions_path, :class => "btn btn-lg btn-crud"%>
<%else%>
	<%= link_to "Volver", presentation_about_path, :class => "btn btn-lg btn-crud"%>
<%end%>
